import styles from './scorescreen.module.css'

function ScoreScreen({ score }) {
	

	return (
		<div className={styles.score_screen}> 
			<p className={styles.username}>Crownless</p>				
			<div className={styles.type_info}>
				<p>wpm</p>
				<p>{score.wpm}</p>	
				<p>time 30</p>
			</div>
		</div>
	);
}

export default ScoreScreen
